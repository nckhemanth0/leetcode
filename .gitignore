# ── Personal local folder (never pushed) ──
# Private notes, scratch solutions, local experiments.
personal/

# ── Secrets — never commit LeetCode session ──
.env
.env.*
debug.log
*.cookie
cookies.txt

# ── Python ──
__pycache__/
*.py[cod]
.venv/
venv/

# ── OS / editor ──
.DS_Store
.idea/
*.swp

# organize.py topic cache — committed by CI automatically
# .topic_cache.json
